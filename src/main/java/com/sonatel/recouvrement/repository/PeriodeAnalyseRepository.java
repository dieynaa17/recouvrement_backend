package com.sonatel.recouvrement.repository;

import com.sonatel.recouvrement.model.PeriodeAnalyse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeriodeAnalyseRepository extends JpaRepository<PeriodeAnalyse, Long> {
}
